# Diagnostic pin escaped under local queue pressure

Run `20260925T024450Z-8412191b` used the frozen paired-host setup, source `decdb22`. Both eight-H200 hosts started together and six local plus six remote serving qualification requests passed. Four full calibration episodes finished and verified their routes and KV-transfer counters. The fifth episode (`train-0-2-local`, state17local/0remote background requests) completed19 native client requests but failed routing verification: **five local-pinned requests went to the remote decoder**, including both foreground probes. Those fifth-episode measurements are invalid for forced-local calibration. No allowance tuning or held-out policy comparison ran.

The request header carried the encoded **local** decoder identity; the Envoy log recorded the **remote** decoder selected for five requests. The local decoder's nearest real load samples showed eight running requests and six waiting, within0.22seconds of those dispatches. The diagnostic decode profile applied `utilization-detector` before `session-affinity-filter`. The upstream detector's default waiting-queue limit is5 (zero headroom) and it excludes a saturated endpoint if another remains. The session-affinity filter then passes all remaining endpoints when its requested pod is missing. Thus the configured diagnostic pin could escape under precisely the high-load state the calibration needed. This diagnosis is supported by the saved request, route and load evidence plus the corresponding upstream filter code.

The narrow evaluation-only fix puts `diagnostic-pin` before `utilization-detector` in the diagnostic profile. Once pinned, the detector receives only that endpoint and its existing empty-result fallback retains it under saturation. The five evaluated policies retain their original filter order. This changes how controlled calibration pins a route; it does not alter the proposed Go topology filter or the comparison policies. A local renderer test asserts the order. No GPU run of the corrected order has occurred.

Independent cleanup verified all five scoped paid resource types empty at **2026-09-25T03:29:44.601245UTC**. Complete native instance histories give a conservative lifecycle estimate of **$20.60 before tax** (two hosts about$10.10each, support allowance about$0.37), not an invoice. The router project estimate is **$318.79** before tax; **$54.40 remains** from this attempt's$75 ceiling. The single paid attempt is consumed and monitoring is paused. Four verified calibration episodes and the invalid fifth episode are retained separately; a new deployment would still need fresh qualification and a balanced full calibration.
