# Paired GPU serving qualified; calibration stopped on harness JSON type error

Run `20260925T004615Z-a6cf555f`, source `4ad90f0`. One Terraform apply requested both eight-H200 groups at the same timestamp, after the cloud guard acknowledged the shortened two-host deadline. Both hosts reached healthy RUNNING state and both model Pods started together. Six local and six remote real-model P/D qualification requests passed, with expected answers, the packed BHLNC layout and correct local CUDA-IPC/remote RDMA transfer evidence on all four receiving ranks. This validates paired provisioning and serving, not routing-policy performance.

The first calibration episode finished three requests (two foreground, one background) and saved routes, client timings, 53 engine-load samples and transfer counters off-host. The runner then called `pilot.read_json()` on `engine-load.json`. That helper requires a JSON **object**, while the native load observer correctly writes a JSON **array**. The controller raised before marking the trial's transfer verification or attempting further episodes. Using the saved files offline, the corrected array reader parsed all53samples, reconstructed both foreground probe timelines and verified all three request routes and KV-transfer counters. This is one forced-local training episode, not a calibrated allowance, held-out comparison or performance claim.

The broad synthetic rehearsal did not catch this because its controller path disabled real-GPU checks. This was an agent harness mistake. The 285-test local suite passed (282 macOS passes, three unchanged Linux-only skips). A regression test now reads the exact saved 53-sample array, checks the probe timeline, and rejects a JSON object. The rest of the calibration and all policy trials remain unrun. No new cloud attempt was authorized for this repair.

Independent cleanup verified all five scoped paid resource types empty at **2026-09-25T01:30:34.701498UTC**. Complete native instance histories show both GPU hosts created and deleted. Conservative lifecycle estimate: **$20.48 before tax**, with about$10.06 each host and$0.37 support allowance; not an invoice. **$54.52 remains** from this attempt's$75total ceiling. Cumulative router estimate **$298.19 before tax**. The one paid attempt is consumed, automatic monitoring is paused and no further rental is authorized. Completed qualification and first-trial evidence remain saved; a new deployment would still need fresh serving qualification and a full balanced calibration.
